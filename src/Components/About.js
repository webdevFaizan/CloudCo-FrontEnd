import React from 'react';
// import NoteContext from '../Context/Note/NoteContext';
// import NoteState from '../Context/Note/NoteState';

const About = () => {
  // const a=useContext(NoteContext);
  // useEffect(()=>{
  //   a.updateState();
  //   // eslint-disable-next-line
  //   // By adding this comment, we are basically using useEffect as a component did mount method, and if we want to use this useEffect hook as component did update then this can be done by updating the array elements that will have variables that will be tracked regarding, and if any of those variable changes then the useEffect will run, basically like updating.
  // },[])
  return (
    <>
      This is About page
    </>
  )
}

export default About
